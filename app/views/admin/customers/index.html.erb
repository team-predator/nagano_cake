<h2>会員一覧</h2>

<p id="notice"></p>
<div class="container">
  <div class="row">
    <div class="col-md-10">
      <table class="table table-hover table-inveres">
      <thead>
        <tr>
        <th>会員ID</th>
        <th>氏名</th>
        <th>メールアドレス</th>
        <th>ステータス</th>
        </tr>
      </thead>
      <tbody>
        <% @customers.each do |customer| %>
      　<tr>
      　<td><%= customer.id %></td>
      　<td><%= link_to customer.name, admin_customer_path(customer) %></td>
      　<td><%= customer.email %></td>
      　<td><% if customer.is_deleted == true %>有効
      　     <% else customer.is_deleted == false %>退会 
      　     <% end %>
      　</td>
      　</tr>
      　<% end %>
      </tbody>
      </table>
      <div class="center-block">
      <div><%= paginate @customers %></div>
    　</div>
    </div>
  </div>
</div>
